# Dependencies
node_modules/
__pycache__/
*.pyc
*.pyo
.venv/
venv/
env/

# Environment
.env

# Models
models/

# Build output
dist/
build/
*.egg-info/

# OS
.DS_Store
Thumbs.db

# Editors
.vscode/settings.json
*.swp

# ChromaDB (generated at runtime)
**/chroma_db/

# Session activity logs (generated at runtime)
backend/memory/logs/
backend/memory/.starling.pid


# Weather cache (contains location data — do not commit)
backend/memory/weather_cache.json

# Stocks/crypto runtime data (generated at runtime — do not commit)
backend/memory/watchlist.json
backend/memory/stocks_cache.json
backend/memory/stocks_history.json

# Ideas tracker (personal data — do not commit)
backend/memory/ideas.json

# journal entries (personal data — do not commit)
backend/memory/journal/

# YouTube channel metadata cache
backend/memory/youtube_cache.json
backend/memory/youtube_channels.json

# Calendar / Mail data caches and credentials
backend/memory/calendar_cache.json
backend/memory/calendar_credentials.json
backend/memory/mail_credentials.json
backend/memory/mail_cache.json
backend/memory/stocks_history.json


# Soul archive history (SOUL.md tracked; archived snapshots excluded)
backend/memory/soul/SOUL_*.md

# Dream state output (runtime — session summaries, facts, and thoughts; not tracked)
backend/memory/dream/

# Input data (runtime — user inputs and interactions; not tracked)
backend/memory/input/

# Images and Personal Files
assets/dossier_descriptions/
assets/dossier_images/
# Wikipedia RAG data (generated by scripts/ingest_wikipedia.py, not committed to repo)
assets/wikipedia/

# Archived markdown files
assets/archived/

# Plan and Implementation guides for upcoming enhancements (not committed to repo)
plan/

# TODO list (personal notes — do not commit)
TODO.md