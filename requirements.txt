fastapi>=0.111.0
uvicorn[standard]>=0.29.0
httpx>=0.27.0
python-dotenv>=1.0.0
pydantic>=2.0.0
python-multipart>=0.0.9
faster-whisper>=1.0.0
# onnxruntime-gpu MUST be installed last (after fastembed, chromadb, sentence-transformers)
# because those packages pull in the CPU-only `onnxruntime` as a transitive dep, which
# overwrites onnxruntime.dll with a mismatched revision that breaks CUDAExecutionProvider.
# After any full pip install, run: pip install --force-reinstall --no-deps onnxruntime-gpu
onnxruntime-gpu>=1.20.0
# All nvidia-* wheels required for onnxruntime CUDA provider on Windows.
# Without these, the DLL search path on Windows won't find the CUDA libraries.
nvidia-cublas-cu12
nvidia-cuda-runtime-cu12
nvidia-cuda-nvrtc-cu12
nvidia-cufft-cu12
nvidia-curand-cu12
nvidia-cudnn-cu12
kokoro-onnx[gpu]>=0.5.0
soundfile>=0.13.0

# RAG / memory system
chromadb>=0.5.0
rank-bm25>=0.2.2
# fastembed is installed separately AFTER onnxruntime-gpu to avoid the CPU
# onnxruntime package overwriting the GPU one. Do not reorder these lines.
# Install order: pip install onnxruntime-gpu  →  then pip install fastembed
# After full env rebuild: pip install --force-reinstall --no-deps onnxruntime-gpu
fastembed>=0.3.0

# News briefing (Tool 4)
feedparser>=6.0.11

# Stocks & Crypto (Tool 5)
yfinance>=0.2.54
# tzdata is required on Windows for zoneinfo timezone lookups
tzdata>=2024.1

# Weather location geocoding (Tool 3, Enhancement 2)
geopy>=2.4.0

# Wikipedia RAG (Tool 10 — Phase 1)
# sentence-transformers is installed AFTER fastembed to avoid onnxruntime conflicts.
# Install order: pip install onnxruntime-gpu  →  pip install fastembed  →  pip install sentence-transformers
sentence-transformers>=2.7.0
einops>=0.7.0  # required by nomic-ai/nomic-embed-text-v1 custom model code
# Ingestion-only deps (only needed to run scripts/ingest_wikipedia.py once):
mwxml>=0.3.3
mwparserfromhell>=0.6.6
tqdm>=4.66.4
